psutil>=5.9

# pywebview-based UI (tools/gen_modelbin_web/), Forza Writer's only GUI.
# Embeds the OS's WebView2 (Chromium) runtime already present on most
# Windows 10/11 installs. Runs as a local desktop app -- no network server.
pywebview>=6.0

# Font/mesh generation (tools/gen_modelbin.py, tools/gen_fontpack.py,
# forza_writer/primitive_fit.py) — already hard dependencies, previously
# undeclared here.
fontTools>=4.0
mapbox_earcut>=1.0
numpy>=1.24
Pillow>=10.0
shapely>=2.0
uharfbuzz>=0.52

# Optional NVIDIA CUDA acceleration for primitive-composed JSON generation.
# The [ctk] extra supplies the redistributable CUDA runtime/compiler pieces;
# users still need a compatible NVIDIA display driver. CPU generation
# remains available when this is absent. Install with:
#   pip install "cupy-cuda12x[ctk]>=14.1,<15"
# cupy-cuda12x[ctk]>=14.1,<15

# Optional AMD/Intel/NVIDIA acceleration via DirectML — EXPERIMENTAL, unverified
# on real DirectML hardware (see docs/DIRECTML_ACCELERATION.md). Install with:
#   pip install torch torch-directml
# CPU generation remains available when this extra is absent.

# Optional (diagnostics suite): enables event-driven filesystem watching.
# If absent, the filesystem watcher falls back to polling automatically, so
# this is not required. Install with: pip install watchdog
# watchdog>=3.0

# Build-time-only: needed only to build the optional Rust CPU-acceleration
# extension (rust/primitive_fit_rs). Not required to run Forza Writer — it
# falls back to the pure-Python implementation when the extension isn't
# built/installed (see docs/RUST_CPU_ACCELERATION.md). Install with:
#   pip install "maturin>=1.5,<2.0"
# maturin>=1.5,<2.0
