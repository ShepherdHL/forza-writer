# Dependencies needed to run this repo's own test suite. Not required to
# run Forza Writer itself -- see requirements.txt for that.
#
# pytest-xdist distributes tests across separate OS processes (see
# pytest.ini's addopts), which is purely a speed optimization here.
pytest>=8.0
pytest-xdist>=3.5
